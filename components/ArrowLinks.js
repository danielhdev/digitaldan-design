import React from 'react'

const ArrowLinks = ({ children }) => {
    return (
        <div className="herosubtext font-bold text-amber-600 cursor-pointer ml-[1.5vw] pl-[1.5vw] md:pl-0"
        >{children}</div>
    )
}

export default ArrowLinks